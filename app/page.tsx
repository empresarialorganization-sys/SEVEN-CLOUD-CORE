export default function Home() {
  return <main style={{maxWidth:760,margin:"15vh auto",padding:28}}>
    <div style={{fontSize:13,letterSpacing:3,opacity:.6}}>SEVEN · CLOUD CORE</div>
    <h1 style={{fontSize:42,margin:"12px 0"}}>Online.</h1>
    <p style={{opacity:.75,lineHeight:1.6}}>Private browser bridge, MCP endpoint and persistent Vault. No secrets or session data are rendered here.</p>
    <code style={{opacity:.6}}>/mcp · /api/health</code>
  </main>;
}
