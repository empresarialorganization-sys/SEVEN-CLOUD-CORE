# SEVEN v0.8 status

This repository is the deployable Cloud Core source. The Supabase schema/migrations have been validated against a live SEVEN Cloud Core project. Production use still requires Vercel environment secrets, a deployed `/mcp` endpoint, the v0.8 browser extension configuration, and a live pairing/upload/browser round-trip test. No provider secret is committed here.
